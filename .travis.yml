language: c
install: wget https://raw.githubusercontent.com/ocaml/ocaml-ci-scripts/master/.travis-opam.sh
script: bash -ex .travis-opam.sh
sudo: required
env:
  global:
    - PACKAGE="jackline"
    - EXTRA_REMOTES="https://github.com/hannesm/xmpp-opam.git"
    - TESTS=false
matrix:
  include:
#  - os: linux
#    env: OCAML_VERSION=4.07
#  - os: linux
#    env: OCAML_VERSION=4.08
#  - os: osx
#    env: OCAML_VERSION=4.08
  - os: linux
    dist: bionic
    before_install:
      - sudo apt-add-repository -y ppa:avsm/ppa
      - sudo apt-get -y install devscripts equivs lintian fakeroot
    script: bash -ex .travis-build-deb.sh
notifications:
  email: false
